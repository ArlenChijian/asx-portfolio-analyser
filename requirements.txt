# Data pipeline
yfinance>=0.2.40          # Pulls market data from Yahoo Finance
pandas>=2.2.0             # Tabular data manipulation
numpy>=1.26.0             # Numerical computing
requests>=2.31.0          # HTTP requests (for ASX listing scraping)
beautifulsoup4>=4.12.0    # HTML parsing (for ASX listing scraping)
lxml>=5.0.0               # Fast XML/HTML parser used by pandas + bs4

# Analysis
scipy>=1.12.0             # Optimisation (used by portfolio optimiser)
scikit-learn>=1.4.0       # ML utilities (clustering, scaling)

# Notebooks
jupyter>=1.0.0
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.20.0            # Interactive charts (also used in the web UI)

# Database
SQLAlchemy>=2.0.0         # SQL toolkit for talking to SQLite

# Web backend (lightweight; we'll add this if/when needed)
fastapi>=0.110.0
uvicorn>=0.29.0

# AI
anthropic>=0.30.0         # Claude API for natural-language input + explanations
python-dotenv>=1.0.0      # Loads API keys from a .env file
